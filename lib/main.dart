import 'package:flutter/material.dart';
import 'package:intro_flutter_2_custom_component/app.dart';

// Questa è la funzione main, il punto di ingresso per l'applicazione.
void main() {
  // runApp è una funzione Flutter che prende come argomento un Widget
  // che l'applicazione Flutter disegnerà sullo schermo.
  // In questo caso, App() è il widget radice per l'intera applicazione.
  runApp(App());
}
